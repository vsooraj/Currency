namespace CuurencyFormat
{
    class GraphData
    {
        //List<object> dataGoal = new List<object>();
        //List<object> dataActual = new List<object>();
        //List<object> tempList = new List<object>();
        //dataGoal = GetNewMRRSeriesDataGoal(companyId);
        //dataActual = GetNewMRRSeriesDataActual(companyId);
        //tempList = dataGoal.Concat(dataActual).ToList();
        //var currency = APTCommon.Currency.GetCurrency(tempList);
        //currency = currency == "H" ? "" : currency;
        //    dataGoal = APTCommon.Currency.ConvertData(currency, dataGoal);
        //    //List<string> data = GetNewMRRDataForXAxis(companyId);
        //    dataActual = APTCommon.Currency.ConvertData(currency, dataActual);
        //    string currencyFormat = "${c}" + currency;
        //graphOptions.calculable = true;
        //    graphOptions.color = color;

        //    List<XAxis> xAxisList = new List<XAxis>();
        //xAxisList.Add(new XAxis());
        //    xAxisList[0].type = "category";
        //    xAxisList[0].data = GetNewMRRDataForXAxis(companyId);
        //graphOptions.xAxis = xAxisList;

        //    axisLabel.formatter = "{value}" + currency;
    }
}
